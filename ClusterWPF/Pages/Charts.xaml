<Page x:Class="ClusterWPF.Pages.Charts"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:views="clr-namespace:ClusterWPF.Views"
      xmlns:local="clr-namespace:ClusterWPF.Pages"
      mc:Ignorable="d" 
      xmlns:ui="http://schemas.modernwpf.com/2019" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Charts">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Border BorderThickness="0 0 0 1" BorderBrush="{DynamicResource MahApps.Brushes.Button.Border}" Grid.ColumnSpan="2">
            <Label HorizontalAlignment="Center" Padding="10" FontSize="20">Charts</Label>
        </Border>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch">
            <Grid VerticalAlignment="Stretch" Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>
                <views:ChartView Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="400"/>

                <ui:SimpleStackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Stretch">
                    <Border Padding="10" Height="300">
                        <ui:SimpleStackPanel>
                            <Label>Computer1</Label>
                            <Border BorderThickness="2"></Border>
                            
                        </ui:SimpleStackPanel>
                    </Border>
                </ui:SimpleStackPanel>

            </Grid>
        </ScrollViewer>


    </Grid>
</Page>
