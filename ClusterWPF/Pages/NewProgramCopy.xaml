<Page x:Class="ClusterWPF.Pages.NewProgramCopy"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.modernwpf.com/2019" 
      xmlns:local="clr-namespace:ClusterWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NewProgramCopy">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Border BorderThickness="0 0 0 1" BorderBrush="{DynamicResource MahApps.Brushes.Button.Border}" Grid.ColumnSpan="2">
            <Label HorizontalAlignment="Center" Padding="10" FontSize="20">New Program Instance</Label>
        </Border>
        <ui:SimpleStackPanel Spacing="5" Grid.Row="1" Margin="0 10 0 0">
            <ui:SimpleStackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <ui:SimpleStackPanel Margin="40 20 40 20" Grid.Row="0">
                    <Label Padding="10" HorizontalAlignment="Center">Computers:</Label>
                    <ComboBox
                        IsEditable="True"
                        Width="220"
                        Margin="10 0 10 0"
                        x:Name="cbComputers"
                        HorizontalAlignment="Center"
                        ui:ControlHelper.PlaceholderText="Search a program..." />
                </ui:SimpleStackPanel>
                <ui:SimpleStackPanel Margin="40 20 40 20" Grid.Row="0">
                    <Label Padding="10" HorizontalAlignment="Center">Existing programs:</Label>
                    <ComboBox
                    IsEditable="True"
                    Width="220"
                    Margin="10 0 10 0"
                    x:Name="cbPrograms"
                    HorizontalAlignment="Center"
                    ui:ControlHelper.PlaceholderText="Search a program..." />
                </ui:SimpleStackPanel>
            </ui:SimpleStackPanel>
                <Button x:Name="btnRunProgram" HorizontalAlignment="Center" Grid.Row="1" Cursor="Hand" Margin="0 0 0 10">Run Program</Button>
            <ui:SimpleStackPanel>
                <TextBox
                            ui:ControlHelper.PlaceholderText="New program's name..."
                            x:Name="tbNewProgramName"
                            TextAlignment="Center"
                            Width="200px">
                </TextBox>
                <Button x:Name="btnRunNewProgram" HorizontalAlignment="Center" Grid.Row="1" Width="200px" Cursor="Hand">Run a new program</Button>
            </ui:SimpleStackPanel>
        </ui:SimpleStackPanel>
    </Grid>
</Page>
