<Page x:Class="ClusterWPF.Pages.StopProgramCopy"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClusterWPF.Pages"
      xmlns:ui="http://schemas.modernwpf.com/2019" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StopProgramCopy">

    <Grid>
        <ui:SimpleStackPanel Width="400px" Spacing="20">
            <Label HorizontalAlignment="Center">Stop program copy</Label>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="60"></RowDefinition>
                </Grid.RowDefinitions>
                <ui:SimpleStackPanel Margin="40 20 40 20" Grid.Row="0">
                    <Label Padding="10" HorizontalAlignment="Center">Existing Program copies:</Label>
                    
                    <TextBox
                    Margin="10 0 10 0"
                    ui:ControlHelper.PlaceholderText="Search..."
                    x:Name="tbSearchRemoveableProgramCopies"
                    TextAlignment="Center">
                    </TextBox>
                    <ListBox x:Name="lbRemovableProgramCopies">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FullName}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ui:SimpleStackPanel>
                <Button x:Name="btnRemoveProgramCopy" HorizontalAlignment="Center" Grid.Row="1">Remove program copy</Button>
            </Grid>
        </ui:SimpleStackPanel>
    </Grid>
</Page>
