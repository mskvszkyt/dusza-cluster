<Page x:Class="ClusterWPF.Pages.ComputerRemove"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClusterWPF.Pages"
      xmlns:ui="http://schemas.modernwpf.com/2019" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ComputerRemove">

    <Grid>
        <ui:SimpleStackPanel Width="400" Spacing="20" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label HorizontalAlignment="Center" FontSize="16" FontWeight="Bold">Remove Computer</Label>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="60"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Padding="10" HorizontalAlignment="Center">Computers:</Label>

                <ui:SimpleStackPanel Margin="20" Grid.Row="1">
                    <TextBox x:Name="tbSearchRemoveableComputers"
                             Margin="0 0 0 10"
                             ui:ControlHelper.PlaceholderText="Search..."
                             TextAlignment="Center"
                             TextChanged="TbSearchRemoveableComputers_TextChanged"/>

                    <ListBox x:Name="lbRemovableComputers"
                             Height="200"
                             SelectionMode="Single"/>
                </ui:SimpleStackPanel>

                <Button x:Name="btnRemoveComputer"
                        Grid.Row="2"
                        HorizontalAlignment="Center"
                        Width="150"
                        Click="BtnRemoveComputer_Click">
                    Remove Computer
                </Button>
            </Grid>
        </ui:SimpleStackPanel>
    </Grid>
</Page>